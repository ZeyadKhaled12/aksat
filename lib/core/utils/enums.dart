enum RequestState{
  loading,
  loaded,
  error
}


enum AncInfoKeys{
  customerName,
  phoneNumber,
  productType,
  totalPrice,
  paidUpPrice,
  valueOfKst,
  kstSystem,
  startDate,
  extra
}

enum AksatStatus{
  late,
  done,
  close,
  normal,
  all,
}